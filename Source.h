#ifndef SOURCE_H
#define SOURCE_H
//just an enumeration used to split keyboard input and read from file input
//there is a different implementation on the case that a citizen is not vacinated and then needs to be vacinated
enum Source {
    DISK,
    KEYBOARD
};
#endif /* SOURCE_H */

